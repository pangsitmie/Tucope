<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.roundbytes.myportfolio">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myportfolio"
        android:usesCleartextTraffic="true">
        <activity android:name=".IntroActivity"/>
        <activity android:name=".activity.WelcomeActivity" />
        <activity android:name=".crypto.AddNewCryptoActivity" />
        <activity android:name=".StockHistoryActivity" />
        <activity android:name=".CryptoHistoryActivity" />
        <activity android:name=".activity.AddTransaction" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity.SignupActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>